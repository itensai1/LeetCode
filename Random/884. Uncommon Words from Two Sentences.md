### codes by *@itensai1*

> C#

```cs
public class Solution {
    public string[] UncommonFromSentences(string s1, string s2)
    {
        Dictionary<string, int> dict = new Dictionary<string, int>();

        string s1s2 = s1 + ' ' + s2;

        string[] arr = s1s2.Split(" ");
        
        foreach (string word in arr)
            if (dict.ContainsKey(word)) dict[word]++;
            else dict.Add(word, 1);

        List<string> answer = new List<string>();

        foreach (var pair in dict)
            if(pair.Value == 1)answer.Add(pair.Key);

        return answer.ToArray();
    }
}
```

> C++

```cpp
class Solution {
public:
    vector<string> uncommonFromSentences(string s1, string s2) {
        
        map<string ,int>map;
        
        string s1s2 = s1 + " " + s2 + " ", tmp = "";

        for (char c: s1s2) {
            if (c == ' ')map[tmp]++,tmp = "";
            else tmp += c ;
        }

        vector<string> answer;

        for(auto pair:map)
            if(pair.second == 1)answer.push_back(pair.first);

        return answer;
    }
};
```

> Python

```py
class Solution:
    def uncommonFromSentences(self, s1: str, s2: str) -> List[str]:
        
        s1s2:str = s1 + " " + s2
        arr:List[str] = s1s2.split(" ")

        mydict:dict = {}

        for word in arr:
            if mydict.get(word):
                mydict[word]+=1
            else:
                mydict[word] = 1

        answer:List[str] = []

        for key in mydict.keys():
            if mydict[key] == 1:
                answer.append(key)
        
        return answer

```


<!-- 17-09-2024 -->
